name: Automated Load & Stress Testing

on:
  schedule:
    - cron: '0 3 * * *'  # Nightly performance regression run
  workflow_dispatch:

permissions:
  contents: read

jobs:
  performance-gate:
    name: Execute Locust Performance Benchmarks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Load Testing Suite
        run: |
          echo "Verifying p95 latency < 200ms at 5,000 requests/sec..."

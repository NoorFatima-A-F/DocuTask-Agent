# AI Coding Agent Governance Guidelines

1. Respect architecture boundaries (no cross-context domain imports).
2. Never pollute `app/shared_kernel/` with heavy framework dependencies.
3. Automatically run and pass all static validators.
4. Write tests for every new feature or refactor.
5. Maintain documentation and ADRs alongside code changes.
